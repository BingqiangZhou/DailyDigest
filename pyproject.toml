[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[project]
name = "daily-digest"
version = "0.1.0"
requires-python = ">=3.11"
